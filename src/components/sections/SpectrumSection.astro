---
import type { Locale } from "../../i18n/routes";
import { siteCopy } from "../../content/siteCopy";
import { SpectrumExplorer } from "../interactive/SpectrumExplorer";
import DeepDive from "../DeepDive.astro";

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const copy = siteCopy[locale];
const s = copy.sections.spectrum;
---

<section class="section" id="spectrum">
  <div class="section__inner">
    <p class="eyebrow">{s.eyebrow}</p>
    <h2 class="section-title">{s.heading}</h2>
    <p class="lead">{s.lead}</p>
    {s.body.map((paragraph) => <p>{paragraph}</p>)}

    <SpectrumExplorer client:visible labels={copy.interactive.spectrumExplorer} />

    {s.deepDive && <DeepDive deepDive={s.deepDive} />}
  </div>
</section>
